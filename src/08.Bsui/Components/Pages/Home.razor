@page "/"
@attribute [Authorize]
@inject NavigationManager NavigationManager

@code {
    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender)
        {
            NavigationManager.NavigateTo(AppRoutes.TicketList, forceLoad: true);
        }
    }
}
